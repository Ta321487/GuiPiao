<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:system="clr-namespace:System;assembly=mscorlib">

    <!-- ============================================ -->
    <!-- 浅色主题颜色定义 -->
    <!-- ============================================ -->

    <!-- 主背景色 -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="ContentBackgroundBrush" Color="#F5F5F5" />
    <SolidColorBrush x:Key="PanelBackgroundBrush" Color="#F8F8F8" />

    <!-- 前景色（文本） -->
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#212121" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#757575" />
    <SolidColorBrush x:Key="TextDisabledBrush" Color="#BDBDBD" />

    <!-- 通用禁用状态透明度 -->
    <system:Double x:Key="DisabledOpacity">0.25</system:Double>

    <!-- 日历非当前月日期颜色 -->
    <SolidColorBrush x:Key="CalendarOtherMonthBrush" Color="#AAAAAA" />

    <!-- 边框和分割线 -->
    <SolidColorBrush x:Key="BorderBrush" Color="#E0E0E0" />
    <SolidColorBrush x:Key="DividerBrush" Color="#EEEEEE" />

    <!-- 控件背景 -->
    <SolidColorBrush x:Key="ControlBackgroundBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="ControlHoverBrush" Color="#F0F0F0" />
    <SolidColorBrush x:Key="ControlPressedBrush" Color="#E0E0E0" />
    <SolidColorBrush x:Key="ControlDisabledBrush" Color="#F5F5F5" />

    <!-- 菜单和工具栏 -->
    <SolidColorBrush x:Key="MenuBackgroundBrush" Color="#F0F0F0" />
    <SolidColorBrush x:Key="MenuHoverBrush" Color="#E0E0E0" />
    <SolidColorBrush x:Key="StatusBarBackgroundBrush" Color="#F0F0F0" />

    <!-- 滚动条样式 -->
    <SolidColorBrush x:Key="ScrollBarTrackBrush" Color="#E8E8E8" />
    <SolidColorBrush x:Key="ScrollBarThumbBrush" Color="#BDBDBD" />

    <!-- DataGrid 样式 -->
    <SolidColorBrush x:Key="DataGridBackgroundBrush" Color="#FFFFFF" />
    <SolidColorBrush x:Key="DataGridHeaderBackgroundBrush" Color="#F0F0F0" />
    <SolidColorBrush x:Key="DataGridHeaderBorderBrush" Color="#E0E0E0" />
    <SolidColorBrush x:Key="DataGridRowHoverBrush" Color="#F5F5F5" />
    <SolidColorBrush x:Key="DataGridRowSelectedBrush" Color="#E3F2FD" />
    <SolidColorBrush x:Key="DataGridAlternatingRowBrush" Color="#FAFAFA" />
    <SolidColorBrush x:Key="DataGridTextBrush" Color="#212121" />

    <!-- 强调色（由 ThemeManager 动态设置） -->
    <!-- AccentBrush：填充色（选中底、主按钮底、进度条） -->
    <!-- AccentForegroundBrush：浅色底上的强调文字/描边（对齐 WinUI SystemAccentColorDark1） -->
    <SolidColorBrush x:Key="AccentBrush" Color="#0078D4" />
    <SolidColorBrush x:Key="PrimaryBrush" Color="#0078D4" />
    <SolidColorBrush x:Key="AccentForegroundBrush" Color="#005A9E" />
    <SolidColorBrush x:Key="AccentHoverBrush" Color="#106EBE" />
    <SolidColorBrush x:Key="AccentPressedBrush" Color="#005A9E" />
    <SolidColorBrush x:Key="AccentTextBrush" Color="#FFFFFF" />
    <Color x:Key="AccentColor">#0078D4</Color>

    <!-- 功能色 -->
    <SolidColorBrush x:Key="SuccessBrush" Color="#28A745" />
    <SolidColorBrush x:Key="WarningBrush" Color="#FD7E14" />
    <!-- OCR/导入字段高亮：浅黄底，保证深色字可读 -->
    <SolidColorBrush x:Key="ImportFieldHighlightBrush" Color="#FFF3CD" />
    <SolidColorBrush x:Key="ErrorBrush" Color="#DC3545" />
    <SolidColorBrush x:Key="InfoBrush" Color="#17A2B8" />

    <!-- 车票报销凭证预览：除票号红色外正文均为黑字 -->
    <LinearGradientBrush x:Key="TicketVoucherBlueSurfaceBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#D4EBF5" Offset="0" />
        <GradientStop Color="#E8F6FC" Offset="1" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="TicketVoucherWarmSurfaceBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#F5D4D4" Offset="0" />
        <GradientStop Color="#FCE8E8" Offset="1" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="TicketVoucherSerialTextBrush" Color="#C62828" />
    <SolidColorBrush x:Key="TicketVoucherPrimaryTextBrush" Color="#000000" />
    <SolidColorBrush x:Key="TicketVoucherSecondaryTextBrush" Color="#000000" />
    <SolidColorBrush x:Key="TicketVoucherMutedTextBrush" Color="#000000" />
    <SolidColorBrush x:Key="TicketVoucherSubtleTextBrush" Color="#000000" />
    <SolidColorBrush x:Key="TicketVoucherAccentBrush" Color="#1565C0" />
    <SolidColorBrush x:Key="TicketVoucherWatermarkBrush" Color="#1A6B9C" />
    <SolidColorBrush x:Key="TicketVoucherCardBorderBrush" Color="#7FA8C4" />
    <SolidColorBrush x:Key="TicketVoucherFooterBackgroundBrush" Color="#E8EEF2" />
    <SolidColorBrush x:Key="TicketVoucherFooterForegroundBrush" Color="#000000" />
    <SolidColorBrush x:Key="TicketVoucherQrPlaceholderBackgroundBrush" Color="#ECEFF1" />
    <SolidColorBrush x:Key="TicketVoucherQrPlaceholderBorderBrush" Color="#90A4AE" />
    <SolidColorBrush x:Key="TicketVoucherQrPlaceholderForegroundBrush" Color="#000000" />
    <SolidColorBrush x:Key="TicketVoucherDisclaimerBorderBrush" Color="#000000" />

    <!-- ============================================ -->
    <!-- Tooltip 样式 - 适配浅色模式，响应式字体大小 -->
    <!-- ============================================ -->
    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="FontSize">
            <Setter.Value>
                <Binding Source="{StaticResource BaseFontSize}" Converter="{StaticResource FontSizeOffsetConverter}"
                         ConverterParameter="-2" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ============================================ -->
    <!-- 控件样式 - 使用主题色 -->
    <!-- ============================================ -->

    <!-- CheckBox 选中状态使用主题色 -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <BulletDecorator Background="Transparent" SnapsToDevicePixels="true">
                        <BulletDecorator.Bullet>
                            <Border x:Name="checkBoxBorder"
                                    Width="16" Height="16"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1"
                                    Background="{TemplateBinding Background}">
                                <Path x:Name="checkMark"
                                      Width="10" Height="10"
                                      Stretch="Uniform"
                                      Fill="{DynamicResource AccentBrush}"
                                      Data="M 0 5 L 3 8 L 8 0"
                                      Stroke="{DynamicResource AccentBrush}"
                                      StrokeThickness="2"
                                      Visibility="Collapsed" />
                            </Border>
                        </BulletDecorator.Bullet>
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </BulletDecorator>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="true">
                            <Setter TargetName="checkMark" Property="Visibility" Value="Visible" />
                            <Setter TargetName="checkBoxBorder" Property="BorderBrush"
                                    Value="{DynamicResource AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="checkBoxBorder" Property="BorderBrush"
                                    Value="{DynamicResource AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabledBrush}" />
                            <Setter Property="Opacity" Value="{DynamicResource DisabledOpacity}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- RadioButton 选中状态使用主题色 -->
    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <BulletDecorator Background="Transparent" SnapsToDevicePixels="true">
                        <BulletDecorator.Bullet>
                            <Grid Width="16" Height="16">
                                <Ellipse x:Name="radioButtonBorder"
                                         Fill="{TemplateBinding Background}"
                                         Stroke="{TemplateBinding BorderBrush}"
                                         StrokeThickness="1" />
                                <Ellipse x:Name="radioButtonCheck"
                                         Fill="{DynamicResource AccentBrush}"
                                         Width="8" Height="8"
                                         Visibility="Collapsed" />
                            </Grid>
                        </BulletDecorator.Bullet>
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </BulletDecorator>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="true">
                            <Setter TargetName="radioButtonCheck" Property="Visibility" Value="Visible" />
                            <Setter TargetName="radioButtonBorder" Property="Stroke"
                                    Value="{DynamicResource AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="radioButtonBorder" Property="Stroke"
                                    Value="{DynamicResource AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabledBrush}" />
                            <Setter Property="Opacity" Value="{DynamicResource DisabledOpacity}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Slider 使用主题色 -->
    <Style TargetType="Slider">
        <Setter Property="Background" Value="{DynamicResource BorderBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
    </Style>

    <!-- ProgressBar：全应用统一为启动页细条（4px / 强调色 / 无边框） -->
    <Style TargetType="ProgressBar">
        <Setter Property="Height" Value="4" />
        <Setter Property="MinHeight" Value="4" />
        <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid x:Name="TemplateRoot" ClipToBounds="True" MinHeight="4">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="2" />
                        <!-- PART_Track：确定进度用其 ActualWidth 计算填充 -->
                        <Border x:Name="PART_Track" Background="Transparent" />
                        <Border x:Name="PART_Indicator"
                                Background="{TemplateBinding Foreground}"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Stretch"
                                CornerRadius="2" />
                        <Grid x:Name="IndeterminateRoot"
                              Visibility="Collapsed"
                              ClipToBounds="True">
                            <Border Width="96"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Stretch"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="2">
                                <Border.RenderTransform>
                                    <TranslateTransform x:Name="IndeterminateTransform" X="-100" />
                                </Border.RenderTransform>
                            </Border>
                        </Grid>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsIndeterminate" Value="True">
                            <Setter TargetName="PART_Indicator" Property="Visibility" Value="Collapsed" />
                            <Setter TargetName="IndeterminateRoot" Property="Visibility" Value="Visible" />
                            <Trigger.EnterActions>
                                <BeginStoryboard x:Name="IndeterminateStoryboard">
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="IndeterminateTransform"
                                                         Storyboard.TargetProperty="X"
                                                         From="-100"
                                                         To="900"
                                                         Duration="0:0:1.4" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="IndeterminateStoryboard" />
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox 选中项使用主题色 -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            Padding="8,4">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="border" Property="Background"
                                    Value="{DynamicResource AccentHoverBrush}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ListBox 选中项使用主题色 -->
    <Style TargetType="ListBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            Padding="8,4">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="border" Property="Background"
                                    Value="{DynamicResource AccentHoverBrush}" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TabControl 选中Tab使用主题色 -->
    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="0,0,0,2"
                            Padding="12,8">
                        <ContentPresenter ContentSource="Header" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="border" Property="BorderBrush"
                                    Value="{DynamicResource AccentHoverBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- MenuItem 完整模板在 AppControlStyles；主题勿再声明隐式 MenuItem，否则会盖掉模板导致下拉变系统浅色底+主题浅色字 -->

    <!-- DataGrid 选中行和悬停行使用主题色 -->
    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="true">
                <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                <Setter Property="Foreground" Value="White" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="true">
                <Setter Property="Background" Value="{DynamicResource AccentHoverBrush}" />
                <Setter Property="Foreground" Value="White" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>