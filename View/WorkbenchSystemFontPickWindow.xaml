<Window x:Class="GuiPiao.View.WorkbenchSystemFontPickWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择系统字体"
        Width="420" Height="480"
        MinWidth="360" MinHeight="360"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource WindowBackgroundBrush}"
        BorderBrush="{DynamicResource BorderBrush}"
        BorderThickness="1"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,8"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   Text="搜索已安装字体" />

        <TextBox x:Name="FilterBox" Grid.Row="1" Margin="0,0,0,8" Padding="8,6"
                 Foreground="{DynamicResource TextPrimaryBrush}"
                 Background="{DynamicResource ControlBackgroundBrush}"
                 BorderBrush="{DynamicResource BorderBrush}"
                 TextChanged="FilterBox_OnTextChanged"
                 ToolTip="搜索" />

        <ListBox x:Name="FontList" Grid.Row="2" Margin="0,0,0,12"
                 BorderBrush="{DynamicResource BorderBrush}"
                 Background="{DynamicResource ControlBackgroundBrush}"
                 Foreground="{DynamicResource TextPrimaryBrush}"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 MouseDoubleClick="FontList_OnMouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontFamily="{Binding}" FontSize="14" Margin="2,3"
                               TextTrimming="CharacterEllipsis" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Style="{StaticResource PrimaryButtonStyle}" MinWidth="80" Margin="0,0,8,0"
                    IsDefault="True" Click="OkButton_OnClick" />
            <Button Content="取消" Style="{StaticResource StandardButtonStyle}" MinWidth="80"
                    IsCancel="True" Click="CancelButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
