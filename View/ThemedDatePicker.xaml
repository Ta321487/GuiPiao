<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:view="clr-namespace:GuiPiao.View"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- 日历按钮样式 -->
    <Style x:Key="CalendarDayButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="Width" Value="32" />
        <Setter Property="Height" Value="32" />
        <Setter Property="FontSize" Value="{DynamicResource BaseFontSize}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource AccentBrush}"
                            BorderThickness="0"
                            CornerRadius="16">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentHoverBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentPressedBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 月份/年份按钮样式 -->
    <Style x:Key="CalendarMonthYearButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="Width" Value="56" />
        <Setter Property="Height" Value="56" />
        <Setter Property="FontSize" Value="{DynamicResource BaseFontSize}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource AccentBrush}"
                            BorderThickness="0"
                            CornerRadius="28">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentHoverBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentPressedBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 导航按钮样式 -->
    <Style x:Key="CalendarNavButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="2">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource ControlHoverBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentPressedBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ThemedDatePicker 默认样式 -->
    <Style TargetType="view:ThemedDatePicker">
        <Setter Property="Background" Value="{DynamicResource ControlBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="MinWidth" Value="120" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="view:ThemedDatePicker">
                    <Grid>
                        <!-- 主输入框区域 -->
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <!-- 日期显示文本框 -->
                                <TextBox x:Name="PART_TextBox"
                                         Grid.Column="0"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Foreground="{TemplateBinding Foreground}"
                                         CaretBrush="{TemplateBinding Foreground}"
                                         FontSize="{DynamicResource BaseFontSize}"
                                         Padding="6,0"
                                         VerticalContentAlignment="Center"
                                         HorizontalContentAlignment="Left"
                                         IsReadOnly="True"
                                         IsReadOnlyCaretVisible="False"
                                         Focusable="False" />

                                <!-- 下拉按钮 -->
                                <Button x:Name="PART_DropDownButton"
                                        Grid.Column="1"
                                        Width="24"
                                        Height="24"
                                        Margin="0,0,2,0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Focusable="False">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    BorderThickness="0">
                                                <Path x:Name="Arrow"
                                                      Data="M0,0 L4,4 L8,0 Z"
                                                      Fill="{DynamicResource TextSecondaryBrush}"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      Stretch="Uniform"
                                                      Width="8"
                                                      Height="4" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Arrow" Property="Fill" Value="{DynamicResource AccentBrush}" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                        </Border>

                        <!-- 下拉日历弹窗 -->
                        <Popup x:Name="PART_Popup"
                               Placement="Bottom"
                               PlacementTarget="{Binding ElementName=Border}"
                               IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                               StaysOpen="False"
                               AllowsTransparency="True"
                               PopupAnimation="Fade">
                            <Border Background="{DynamicResource ControlBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Margin="4"
                                    Padding="8">
                                <Grid Width="240">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <!-- 标题栏：年月和导航按钮 -->
                                    <Grid Grid.Row="0" Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <!-- 上一年 -->
                                        <Button x:Name="PART_PreviousYearButton"
                                                Grid.Column="0"
                                                Width="24"
                                                Height="24"
                                                Style="{StaticResource CalendarNavButtonStyle}"
                                                Content="&#xE971;"
                                                FontFamily="Segoe MDL2 Assets"
                                                FontSize="{DynamicResource SmallFontSize}"
                                                Visibility="Collapsed" />

                                        <!-- 上一月 -->
                                        <Button x:Name="PART_PreviousButton"
                                                Grid.Column="0"
                                                Width="28"
                                                Height="28"
                                                Style="{StaticResource CalendarNavButtonStyle}"
                                                Content="&#xE76B;"
                                                FontFamily="Segoe MDL2 Assets"
                                                FontSize="{DynamicResource BaseFontSize}" />

                                        <!-- 年月标题按钮 -->
                                        <Button x:Name="PART_HeaderButton"
                                                Grid.Column="2"
                                                Style="{StaticResource CalendarNavButtonStyle}"
                                                FontWeight="Bold"
                                                HorizontalAlignment="Center"
                                                Content="{Binding HeaderText, RelativeSource={RelativeSource TemplatedParent}}" />

                                        <!-- 下一月 -->
                                        <Button x:Name="PART_NextButton"
                                                Grid.Column="3"
                                                Width="28"
                                                Height="28"
                                                Style="{StaticResource CalendarNavButtonStyle}"
                                                Content="&#xE76C;"
                                                FontFamily="Segoe MDL2 Assets"
                                                FontSize="12" />

                                        <!-- 下一年 -->
                                        <Button x:Name="PART_NextYearButton"
                                                Grid.Column="4"
                                                Width="24"
                                                Height="24"
                                                Style="{StaticResource CalendarNavButtonStyle}"
                                                Content="&#xE972;"
                                                FontFamily="Segoe MDL2 Assets"
                                                FontSize="10"
                                                Visibility="Collapsed" />
                                    </Grid>

                                    <!-- 星期标题（仅在日期视图显示） -->
                                    <Grid x:Name="PART_WeekDaysGrid" Grid.Row="1" Margin="0,0,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="日" HorizontalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{DynamicResource SmallFontSize}" />
                                        <TextBlock Grid.Column="1" Text="一" HorizontalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{DynamicResource SmallFontSize}" />
                                        <TextBlock Grid.Column="2" Text="二" HorizontalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{DynamicResource SmallFontSize}" />
                                        <TextBlock Grid.Column="3" Text="三" HorizontalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{DynamicResource SmallFontSize}" />
                                        <TextBlock Grid.Column="4" Text="四" HorizontalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{DynamicResource SmallFontSize}" />
                                        <TextBlock Grid.Column="5" Text="五" HorizontalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{DynamicResource SmallFontSize}" />
                                        <TextBlock Grid.Column="6" Text="六" HorizontalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="{DynamicResource SmallFontSize}" />
                                    </Grid>

                                    <!-- 日期视图 -->
                                    <UniformGrid x:Name="PART_DaysGrid" Grid.Row="2" Columns="7" Rows="6">
                                        <!-- 日期按钮将由代码动态生成 -->
                                    </UniformGrid>

                                    <!-- 月份视图 -->
                                    <UniformGrid x:Name="PART_MonthsGrid" Grid.Row="2" Columns="4" Rows="3" Visibility="Collapsed">
                                        <!-- 月份按钮将由代码动态生成 -->
                                    </UniformGrid>

                                    <!-- 年份视图 -->
                                    <UniformGrid x:Name="PART_YearsGrid" Grid.Row="2" Columns="4" Rows="3" Visibility="Collapsed">
                                        <!-- 年份按钮将由代码动态生成 -->
                                    </UniformGrid>
                                </Grid>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
