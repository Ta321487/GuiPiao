<Window x:Class="GuiPiao.View.MapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:GuiPiao.Controls"
        xmlns:icons="clr-namespace:GuiPiao.Icons"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="车票地图"
        Height="800" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Window.Resources>
        <!-- 地图窗口工具栏按钮样式 - 基于 StandardButtonStyle 但更适合工具栏 -->
        <Style x:Key="MapToolbarButtonStyle" BasedOn="{StaticResource StandardButtonStyle}" TargetType="Button">
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Margin" Value="3,0" />
            <Setter Property="MinHeight" Value="28" />
        </Style>

        <Style x:Key="MapFilterAllButtonStyle" BasedOn="{StaticResource MapToolbarButtonStyle}" TargetType="Button">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ActiveStatusFilter}" Value="All">
                    <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="MapFilterCompletedButtonStyle" BasedOn="{StaticResource MapToolbarButtonStyle}" TargetType="Button">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ActiveStatusFilter}" Value="Completed">
                    <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="MapFilterPendingButtonStyle" BasedOn="{StaticResource MapToolbarButtonStyle}" TargetType="Button">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ActiveStatusFilter}" Value="Pending">
                    <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="MapFilterRefundedButtonStyle" BasedOn="{StaticResource MapToolbarButtonStyle}" TargetType="Button">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ActiveStatusFilter}" Value="RefundedRescheduled">
                    <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource AccentTextBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <WrapPanel Margin="10">
                <Button Style="{StaticResource MapToolbarButtonStyle}"
                        Command="{Binding RefreshDataCommand}" Margin="0,0,5,5">
                    <controls:IconLabel Glyph="{x:Static icons:AppIcons.Refresh}" Text="刷新数据" />
                </Button>
                <Button Style="{StaticResource MapFilterAllButtonStyle}"
                        Command="{Binding ShowAllCommand}" Margin="0,0,5,5">
                    <controls:IconLabel Glyph="{x:Static icons:AppIcons.AreaChart}" Text="显示全部" />
                </Button>
                <Button Style="{StaticResource MapFilterCompletedButtonStyle}"
                        Command="{Binding FilterCompletedCommand}" Margin="0,0,5,5">
                    <controls:IconLabel Glyph="{x:Static icons:AppIcons.Accept}" Text="已完成行程" />
                </Button>
                <Button Style="{StaticResource MapFilterPendingButtonStyle}"
                        Command="{Binding FilterPendingCommand}" Margin="0,0,5,5">
                    <controls:IconLabel Glyph="{x:Static icons:AppIcons.Clock}" Text="未出行行程" />
                </Button>
                <Button Style="{StaticResource MapFilterRefundedButtonStyle}"
                        Command="{Binding FilterRefundedCommand}" Margin="0,0,5,5">
                    <controls:IconLabel Glyph="{x:Static icons:AppIcons.Block}" Text="已退票/改签" />
                </Button>
                <Separator Margin="5,0,10,5" Background="{DynamicResource BorderBrush}" Style="{x:Null}" Width="1" />
                <Button Style="{StaticResource MapToolbarButtonStyle}"
                        Command="{Binding OpenSettingsCommand}" Margin="0,0,0,5">
                    <controls:IconLabel Glyph="{x:Static icons:AppIcons.Color}" Text="打开设置" />
                </Button>
            </WrapPanel>
        </Border>

        <!-- 地图区域 -->
        <Border Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0">
            <wv2:WebView2 x:Name="MapWebView"
                          NavigationCompleted="MapWebView_NavigationCompleted" />
        </Border>

        <!-- 状态栏 -->
        <StatusBar Grid.Row="2" Background="{DynamicResource StatusBarBackgroundBrush}">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{x:Static icons:AppIcons.StatusCircle}" FontFamily="Segoe MDL2 Assets" Foreground="#28A745" />
                    <TextBlock Text="{Binding StatusMessage}" />
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>