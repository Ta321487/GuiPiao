<UserControl x:Class="GuiPiao.Controls.AutoCompleteTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="200">
    <Grid>
        <TextBox x:Name="InputTextBox"
                 Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="{DynamicResource BaseFontSize}"
                 Padding="4,2"
                 MinHeight="24"
                 PreviewKeyDown="InputTextBox_PreviewKeyDown"/>
        <Popup IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
               PlacementTarget="{Binding ElementName=InputTextBox}"
               Placement="Bottom"
               Width="{Binding ActualWidth, ElementName=InputTextBox}"
               StaysOpen="False"
               AllowsTransparency="True">
            <Border Background="{DynamicResource PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1">
                <ListBox ItemsSource="{Binding Suggestions, RelativeSource={RelativeSource AncestorType=UserControl}}"
                         SelectedIndex="{Binding SelectedIndex, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                         MaxHeight="200"
                         Background="Transparent"
                         BorderThickness="0"
                         MouseLeftButtonUp="SuggestionsList_MouseLeftButtonUp">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"
                                       FontSize="{DynamicResource BaseFontSize}"
                                       Padding="8,4"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Popup>
    </Grid>
</UserControl>
